// Shared between Header (desktop + mobile) and Footer so the link set stays in sync.
export const navLinks = [
  { href: "/", labelKey: "home" },
  { href: "/catalog", labelKey: "catalog" },
  { href: "/brands", labelKey: "brands" },
  { href: "/contacts", labelKey: "contacts" },
] as const
